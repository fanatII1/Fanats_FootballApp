import icons from "./icons"; //get access to all icons ( in object)

import theme, { COLORS, SIZES, FONTS } from "./theme"; //access to appTheme object and access the named exports which are fonts, sizes and color) 

export { icons,  theme, COLORS, SIZES, FONTS }; //add all icons object in one object. add the
